# アニメ・マンガ情報配信システム - 必須依存関係
# Architecture: Python + SQLite + Gmail API + Google Calendar API + OAuth2

# === Core Google APIs ===
google-auth>=2.17.0
google-auth-oauthlib>=1.0.0  
google-auth-httplib2>=0.1.0
google-api-python-client>=2.80.0

# === RSS/フィード処理 ===
feedparser>=6.0.10
beautifulsoup4>=4.12.0  # HTMLパース用（RSS内のHTML処理）
lxml>=4.9.0  # XMLパーサー（高速処理用）

# === HTTP クライアント ===
requests>=2.31.0
aiohttp>=3.8.5
urllib3>=1.26.0  # セキュリティアップデート

# === JSON/GraphQL処理 ===
# json, jsonschema は標準ライブラリ

# === データ処理・分析 ===
pytz>=2023.3  # タイムゾーン処理（Asia/Tokyo対応）
python-dateutil>=2.8.2  # 日付解析

# === 設定管理 ===
pydantic>=2.0.0  # 設定バリデーション（将来の拡張用）
python-dotenv>=1.0.0  # 環境変数管理

# === セキュリティ ===
cryptography>=41.0.0  # OAuth2トークン暗号化

# === ログ・モニタリング ===
# logging, sys, os は Python 標準ライブラリに含まれる

# === 開発・テスト支援 (development only) ===
# pytest>=7.0.0  # requirements-dev.txt で管理

# === Webフレームワーク ===
Flask>=2.3.0  # Web UI用

# === データベース ===
# sqlite3 は Python 標準ライブラリに含まれる

# === その他の標準ライブラリ ===
# json, logging, sqlite3, datetime, pathlib, typing, dataclasses,
# threading, contextlib, hashlib, signal, traceback, argparse