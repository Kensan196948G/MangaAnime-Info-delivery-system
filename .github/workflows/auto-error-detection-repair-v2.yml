name: 自動エラー検知・修復ループシステム v2（最適化版）

on:
  # 30分おきに自動実行
  schedule:
    - cron: '*/30 * * * *'  # 30分ごと

  # 手動実行も可能
  workflow_dispatch:
    inputs:
      max_loops:
        description: '最大ループ回数'
        required: false
        default: '10'
      force_full_repair:
        description: '完全修復を強制（警告も修復）'
        required: false
        type: boolean
        default: false

  # Issueコメントでトリガー
  issue_comment:
    types: [created]

# 同時実行を防ぐ
concurrency:
  group: auto-repair-system
  cancel-in-progress: false

jobs:
  error-detection-and-repair:
    runs-on: ubuntu-latest
    timeout-minutes: 25  # タイムアウトを5分延長
    # Issueコメントが@auto-repairの場合のみ実行
    if: |
      github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@auto-repair'))

    env:
      MAX_LOOPS: ${{ github.event.inputs.max_loops || '10' }}
      REPAIR_INTERVAL: 30  # 秒（GitHub Actions用に短縮）

    steps:
      - name: チェックアウト
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Python環境セットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 依存関係インストール（リトライ付き）
        timeout-minutes: 8
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 5
          max_attempts: 3
          retry_wait_seconds: 10
          command: |
            pip install --upgrade pip
            pip install -r requirements.txt
            pip install -r requirements-dev.txt

      - name: エラー検知・修復ループ実行
        id: repair-loop
        timeout-minutes: 18
        continue-on-error: true  # 失敗しても次のステップへ
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          python scripts/auto_error_repair_loop.py \
            --max-loops "$MAX_LOOPS" \
            --interval "$REPAIR_INTERVAL" \
            --issue-number "${ISSUE_NUMBER:-}" \
            --create-issue-on-failure

      - name: 修復ステータスを判定
        id: check-status
        if: always()
        run: |
          if [ -f repair_summary.json ]; then
            FINAL_STATUS=$(jq -r '.final_status // "unknown"' repair_summary.json)
            ERROR_REDUCTION=$(jq -r '.error_reduction_rate // 0' repair_summary.json)
            CRITICAL_ERRORS=$(jq -r '.critical_errors // 999' repair_summary.json)

            echo "final_status=$FINAL_STATUS" >> $GITHUB_OUTPUT
            echo "error_reduction=$ERROR_REDUCTION" >> $GITHUB_OUTPUT
            echo "critical_errors=$CRITICAL_ERRORS" >> $GITHUB_OUTPUT

            # 成功判定ロジック
            if [ "$FINAL_STATUS" = "success" ] || [ "$FINAL_STATUS" = "partial_success" ]; then
              echo "result=success" >> $GITHUB_OUTPUT
            elif [ "$FINAL_STATUS" = "improved" ] && [ "$CRITICAL_ERRORS" -eq 0 ]; then
              echo "result=success" >> $GITHUB_OUTPUT
            elif [ "$FINAL_STATUS" = "attempted" ]; then
              echo "result=partial" >> $GITHUB_OUTPUT
            else
              echo "result=failed" >> $GITHUB_OUTPUT
            fi
          else
            echo "final_status=error" >> $GITHUB_OUTPUT
            echo "result=failed" >> $GITHUB_OUTPUT
          fi

      - name: 修復結果サマリー作成
        if: always()
        run: |
          echo "## 🔧 自動修復ループ実行結果 v2" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ -f repair_summary.json ]; then
            # JSON から情報を抽出
            FINAL_STATUS=$(jq -r '.final_status // "unknown"' repair_summary.json)
            TOTAL_LOOPS=$(jq -r '.total_loops // 0' repair_summary.json)
            MAX_LOOPS=$(jq -r '.max_loops // 0' repair_summary.json)
            SUCCESS_COUNT=$(jq -r '.successful_repairs // 0' repair_summary.json)
            FAILED_COUNT=$(jq -r '.failed_repairs // 0' repair_summary.json)
            ERROR_REDUCTION=$(jq -r '.error_reduction_rate // 0' repair_summary.json)
            CRITICAL_ERRORS=$(jq -r '.critical_errors // 0' repair_summary.json)
            WARNING_ERRORS=$(jq -r '.warning_errors // 0' repair_summary.json)

            # ステータスバッジ
            case "$FINAL_STATUS" in
              "success")
                BADGE="✅ 完全成功"
                ;;
              "partial_success")
                BADGE="⚠️ 部分的成功（警告のみ残存）"
                ;;
              "improved")
                BADGE="📈 改善（エラー削減率: ${ERROR_REDUCTION}%）"
                ;;
              "attempted")
                BADGE="🔧 修復試行済み"
                ;;
              *)
                BADGE="❌ 失敗"
                ;;
            esac

            echo "### $BADGE" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| 項目 | 値 |" >> $GITHUB_STEP_SUMMARY
            echo "|------|-----|" >> $GITHUB_STEP_SUMMARY
            echo "| ループ数 | $TOTAL_LOOPS / $MAX_LOOPS |" >> $GITHUB_STEP_SUMMARY
            echo "| 成功した修復 | $SUCCESS_COUNT |" >> $GITHUB_STEP_SUMMARY
            echo "| 失敗した修復 | $FAILED_COUNT |" >> $GITHUB_STEP_SUMMARY
            echo "| エラー削減率 | ${ERROR_REDUCTION}% |" >> $GITHUB_STEP_SUMMARY
            echo "| クリティカルエラー | $CRITICAL_ERRORS |" >> $GITHUB_STEP_SUMMARY
            echo "| 警告レベルエラー | $WARNING_ERRORS |" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            # 推奨アクション
            echo "### 📝 推奨アクション" >> $GITHUB_STEP_SUMMARY
            jq -r '.recommendations[] | "- \(.)"' repair_summary.json >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ 修復サマリーが生成されませんでした" >> $GITHUB_STEP_SUMMARY
          fi

      - name: 修復ログをアーティファクトとして保存
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: repair-logs-${{ github.run_number }}
          path: |
            repair_summary.json
            logs/auto_repair_*.log
          retention-days: 30

      - name: 失敗時にIssue作成（クリティカルエラーのみ）
        if: steps.check-status.outputs.result == 'failed' && steps.check-status.outputs.critical_errors != '0'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let summary = '修復サマリーファイルが見つかりません';

            try {
              if (fs.existsSync('repair_summary.json')) {
                const data = JSON.parse(fs.readFileSync('repair_summary.json', 'utf8'));

                const statusBadges = {
                  'success': '✅',
                  'partial_success': '⚠️',
                  'improved': '📈',
                  'attempted': '🔧',
                  'failed': '❌'
                };

                const badge = statusBadges[data.final_status] || '❓';

                summary = `
            ## ${badge} 自動修復失敗レポート

            **実行日時**: ${data.timestamp || new Date().toISOString()}
            **最終ステータス**: ${data.final_status}
            **実行ループ数**: ${data.total_loops || 0} / ${data.max_loops}
            **成功した修復**: ${data.successful_repairs || 0}
            **失敗した修復**: ${data.failed_repairs || 0}
            **エラー削減率**: ${(data.error_reduction_rate || 0).toFixed(1)}%

            ### 🚨 残存エラー
            - **クリティカル**: ${data.critical_errors || 0}
            - **警告**: ${data.warning_errors || 0}

            ### 検出されたエラー
            ${data.detected_errors?.filter(e => e.severity === 'high').map(e => `- **${e.type}**: ${e.message}`).join('\n') || 'なし'}

            ### 推奨アクション
            ${data.recommendations?.map(r => `- ${r}`).join('\n') || '手動での確認が必要です'}

            ---
            🤖 この Issue は自動生成されました（v2: 段階的成功判定）
            `;
              }
            } catch (error) {
              console.error('修復サマリー読み込みエラー:', error);
            }

            // 既存の未解決Issueをチェック
            const existingIssues = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: 'auto-repair',
              state: 'open'
            });

            // 既存Issueが5個未満なら新規作成
            if (existingIssues.data.length < 5) {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: `🚨 自動修復失敗 - ${new Date().toISOString().split('T')[0]}`,
                body: summary,
                labels: ['auto-repair', 'bug', '要確認', 'critical']
              });
            } else {
              console.log('既存のIssueが5個以上あるため、新規作成をスキップしました');
            }

      - name: 成功時にIssueコメント
        if: steps.check-status.outputs.result == 'success' && github.event.issue.number
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let comment = '✅ 自動修復が完了しました';

            try {
              if (fs.existsSync('repair_summary.json')) {
                const data = JSON.parse(fs.readFileSync('repair_summary.json', 'utf8'));
                comment = `
            ## ✅ 自動修復完了 v2

            **最終ステータス**: ${data.final_status}
            **実行ループ数**: ${data.total_loops || 0} / ${data.max_loops}
            **成功した修復**: ${data.successful_repairs || 0}
            **エラー削減率**: ${(data.error_reduction_rate || 0).toFixed(1)}%
            **残存エラー**: クリティカル=${data.critical_errors || 0}, 警告=${data.warning_errors || 0}

            すべてのクリティカルエラーが修復されました 🎉
            `;
              }
            } catch (error) {
              console.error('サマリー読み込みエラー:', error);
            }

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: comment
            });

      - name: 修復結果に基づく終了判定
        if: always()
        run: |
          RESULT="${{ steps.check-status.outputs.result }}"

          if [ "$RESULT" = "success" ]; then
            echo "✅ 修復成功"
            exit 0
          elif [ "$RESULT" = "partial" ]; then
            echo "⚠️ 部分的成功（一部エラー残存）"
            exit 0  # 部分成功も許容
          else
            echo "❌ 修復失敗"
            exit 1
          fi
